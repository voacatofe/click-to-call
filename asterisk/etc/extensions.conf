[general]
static=yes
writeprotect=no
clearglobalvars=no

[globals]
; Variáveis globais podem ser definidas aqui

[from-internal]
; Contexto para chamadas internas, como as dos agentes WebRTC

; Ramal de teste de eco com debug melhorado
exten => 9999,1,NoOp(=== INICIO TESTE DE ECO ===)
 same => n,NoOp(Caller ID: ${CALLERID(all)})
 same => n,NoOp(Channel: ${CHANNEL})
 same => n,NoOp(Remote IP: ${CHANNEL(rtp,src)})
 same => n,Answer()
 same => n,Wait(1)
 same => n,NoOp(=== INICIANDO ECHO ===)
 same => n,Playback(beep)
 same => n,Echo()
 same => n,NoOp(=== ECHO TERMINADO ===)
 same => n,Hangup()

; Ramal de teste de áudio simples
exten => 8888,1,NoOp(=== TESTE DE AUDIO SIMPLES ===)
 same => n,Answer()
 same => n,Wait(1)
 same => n,Playback(hello-world)
 same => n,Wait(2)
 same => n,Hangup()

; Ramal para testar conectividade
exten => 7777,1,NoOp(=== TESTE DE CONECTIVIDADE ===)
 same => n,Answer()
 same => n,SayDigits(12345)
 same => n,Wait(2)
 same => n,Hangup() 